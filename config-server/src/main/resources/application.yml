spring:
  cloud:
    config:
      server:
        git:
          uri: file:C:/Users/****/Desktop/spring-boot-demo/config-repo
server:
  port: 8020
---
spring:
  profiles: docker
  cloud:
    config:
      server:
        git:
          uri: https://git.coding.net/wanyf/config-repo.git
  rabbitmq:
    host: rabbitmq

